"""Shared across both example domains: the permission check and the fake identity provider.
Nothing here is domain-specific and nothing here is neural — it is the toy version of
docs/FRAMEWORK.md §9 gap row E, "permission enforced outside the model."""
