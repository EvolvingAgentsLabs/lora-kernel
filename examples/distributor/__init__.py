"""The distributor reference domain — the second instance of the same skeleton as
examples/school/, on a different schema, per docs/FRAMEWORK.md §9 ("the second domain is the
cheap half, reusing the same skeleton, not new infrastructure"). See examples/README.md."""
