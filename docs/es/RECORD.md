# El registro

**Lo que este repositorio midió entre el 2026-09-06 y el 2026-09-19, incluyendo lo que falló.**
Cada línea nombra su corrida. Un directorio de corrida que no está bajo `results/` en `main` vive en el tag
[`v0.1-foundations`](https://github.com/EvolvingAgentsLabs/lora-kernel/tree/v0.1-foundations),
con la entrada del plan que lo pre-registró. Todo esto es **[ran]** salvo que esté marcado **[read]**,
y todo esto es sobre **suites generadas**: no se midió tráfico real.

No re-derivar lo que está acá. No citar un número de acá sin la salvedad que está al lado.

## 1. Lo que funciona

| hallazgo | número | corrida |
|---|---|---|
| Serving multi-LoRA: un vLLM, una base, cada pedido servido por su propio adaptador; la co-residencia no le cuesta nada al miembro que sirve | identidad `applied`, herramientas alcanzables, stop honrado, en cada miembro | P36–P40, P56 |
| `email-full` — herramientas y criterio en un solo adaptador. Aprendió *cuándo* preguntar, no "preguntar siempre": se salta casi exactamente los mensajes automáticos | 0,989 de exactitud en humanos vs 0,345 de la base; 393 llamadas, 0 rechazadas | P36, P57 |
| `email-full@v1` es reproducible: re-servido y re-entrenado desde el manifiesto, los dos empatan la corrida registrada | 471/475, 471/475, 472/475; 0 y 1 discordante | P57 |
| `desk-commitment@v1` — un segundo miembro sobre la misma bandeja, una pregunta distinta | 240/240 vs 38/240 de la base, 202 : 0; empata su corrida registrada con 0 discordantes | P64 |
| En un 3B, un procedimiento escrito en el contexto no se sigue | base + procedimiento de 914 tokens: 0 llamadas a herramientas en 351/351, 0,601, debajo de la barra de mayoría de 0,655; experto 137 : 1 | P61 |
| Ruteo por región a un modelo de frontera | 0,546 → 0,775, el 38 % de los casos sale; `gemini-3.8-flash` 66/90 en fluidos. *El mecanismo se sostiene; este caso no: la región mandada afuera era una que el pool servía por el camino equivocado — localmente, como le enseñaron, es 90/90 (más abajo)* | P41 |
| El ruteo por pedido empata al ruteo por región | 0,775 = 0,775, 0 mal ruteados, 37,5 % afuera | P62 |
| **El rol dice cuál miembro, nunca si corresponde** — el rol viaja en el id del modelo (`auto:<rol>`), dos políticas declaradas antes de cualquier número, cero GPU sobre los ocho sets del router y el replay de 240 casos | `role_confirmed` (el rol acota las claves a su propia región): nunca más mal ruteados que las claves solas — C 13→8, C2 8→5, E2 51→45 — nada servido bajo un rol equivocado (715/715, 240/240, 120/120 salen), replay empatado en 0,775; **es el default del proxy**. `role_first` (el rol decide salvo que disparen otras claves): el único brazo que sirve todas las paráfrasis, 240/240 contra 109 — y sirve 120 de 120 tareas ajenas sobre el listado propio de un miembro, 25 textos ajenos planos y 131 pedidos bajo el rol equivocado: **falla** | F2 |
| **Un miembro es un directorio declarado, chequeado línea por línea** — `roles/<rol>/role.toml` + `rolepack.lint` | los dos miembros liberados re-expresados: el prompt **es** el `POOL_SYSTEM[miembro]` del proxy, el bloque de herramientas es idéntico byte a byte en cada fila del corpus (598, 600), `POOL` / `REGIONS` / `ROLES` derivables e iguales; el lint falla ante un hash alterado, un prompt que derivó, una superficie reordenada, una clave equivocada; encontró un nombre de suite mal puesto en su propio primer paquete. El miembro de la memoria entra como `unreleased` una vez que el formato puede declarar un atributo de tag (`<search shelf=…>`), y declara un loop — el del árbitro — que la API no puede servir | F3 |
| Una región es su pregunta, no su listado | con clave en marcadores de entrada compartidos 15/60 mal rutea; con clave en la pregunta 60/60, 60/60, 150/150, 140/140 | P64 |
| OpenClaw en vivo, desde una laptop a través de un túnel | 40/40 local, 0 llamadas inventadas, 19/32 turnos humanos llaman a una herramienta, 0,688 contra barra 0,655 ($p = 0.43$, descriptivo) | P63 |
| Un miembro es lo que su corpus enseñó — el bloque *y* el prompt | bajo el prompt de 37 KB del runtime 2/32 turnos llaman a una herramienta, 0,281; bajo el suyo propio, 19/32 | P63 |
| Servido con una superficie de herramientas desconocida, un experto copia etiquetas del bloque | 54 herramientas ofrecidas: 225 de 227 llamadas rechazadas; podado a las suyas: 8 de 1160 | P59 |
| El modo corpus — parar en la etiqueta de cierre, inyectar el resultado, continuar — es cómo se sirve un experto | `email-full` 0,992 de esa forma, 0,808 vía `tool_calls` | P55 |
| vLLM aplica un LoRA sobre un modelo grande cuantizado | `Qwen2.5-32B-AWQ`: compuerta de logprobs 3/3, media \|Δℓ\| 0,22–0,49 nats contra base-vs-base 0,000; la compuerta de texto sola leyó un juguete de 60 pasos como *not applied* | P60 §3b |
| `Qwen3.5-2B/4B` y `Qwen3.8-27B` comparten un espacio de ids | 248.044 ids, 7 sólo del grande (audio/TTS); `<think>` compartido | D0 |
| Ante texto extranjero, un modelo de los corpus es más seguro que el diccionario de palabras clave | conjuntos frescos, escritos después de que el diseño se congeló: texto extranjero con clave y un listado seguido de otra tarea, 128 — el diccionario sirve **59** localmente, el router de n-gramas del corpus **0** | M2 |
| **Un experto que razona, servido como enseñó su corpus.** `fluids-full` sobre sus propias cadenas de 6 a 9 pasos, el resultado escrito inline después del tag de cierre | **90/90**, donde el mismo adaptador sobre los mismos 90 casos puntuó 11/90 por `tool_calls` — **79 : 0** pareado; **24 : 0** contra el 66/90 de la frontera. Chequeado antes de creerlo: 0/90 enunciados evaluados en el corpus, 0/90 respuestas que se repiten, la respuesta final es el último `<calc>` propio del modelo en 89/90 | M7 brazo 0b |
| **…y reentrenado sobre la base nueva del pool no aguanta: la última línea.** `fluids-full` sobre `Qwen3.5-4B`, mismo corpus, misma receta, modo corpus, los mismos 90 casos | **80/90** contra su propio 90/90 — **0 : 10**, $p=0{,}002$, REGRESIÓN; 80 : 0 contra el 4B pelado (0/90, un piso de formato); 21 : 7 contra la frontera. Las diez son cadenas de venturi idénticas a las que el 3B aprobó hasta la línea final, donde el 4B escribe `<answer>x</answer>` — un tag que está en 0 de 600 filas del corpus — en vez de la línea JSON. El número verifica 10/10. **No liberado; la región sigue `out`** | M7 brazo 0c |
| **El pool se muda a otra familia y aguanta.** Los dos miembros liberados reentrenados sobre `Qwen3.5-4B` desde los mismos corpus, receta sin cambios, tensores nombrados para la clase que sirve vLLM, pensamiento apagado | `email-full` **471/475**, sus 471 grabados — empate 1 : 1; `desk-commitment` **240/240** — empate. G1 `applied` en los dos adaptadores de *receta completa*. Las proyecciones de la receta alcanzan sólo 8 de las 32 capas de atención del stack híbrido, y alcanzó. *La base nueva sola es mucho más fuerte en email — humanos 0,632 contra 0,345 del 3B — así que un adaptador tiene menos que agregar ahí; y su 0/240 en el desk es un modelo sin entrenar copiando el marcador `...` del bloque de herramientas 7 veces por caso, que no dice nada sobre la tarea* | M1 |
| **Un procedimiento real tiene margen, y una nota abierta cierra casi todo.** `Qwen3.5-4B`, sin entrenar, sobre 72 preguntas verificables de tres listas IV de Open RN (CC BY 4.0) | orden de pasos + paso siguiente **29/48 a libro cerrado → 45/48 con la nota abierta**; una cantidad que cambió el protocolo de una unidad **0/12 → 12/12** — el modelo sigue a la nota por encima de su prior; goteos 7/12 → 6/12, así que la aritmética se queda con una calculadora. Pareado 29 : 2. *Al brazo a libro abierto se le dio la nota correcta: la recuperación no está probada.* Una base chica **responde sobre** lo que lee (acá) aunque no **actúa bajo** eso (P61) | M5 margen |
| **La primera biblioteca de la memoria existe y se lintea como código** — sin ningún modelo. Tres procedimientos IV de Open RN (CC BY 4.0) como esqueleto + notas de paso, un árbol de wiki, una capa de sitio; las capas resuelven caso ▸ sitio ▸ libro | 94 notas, lint **0 hallazgos**, recorridos del oráculo **72/72**; la compuerta falla cuando se quita un paso. El primer hallazgo del lint fue la propia especificación: un esqueleto de 32 títulos son 230 tokens > 150 | M7-W1 |
| **El árbitro de la memoria corre sobre el loop de modo corpus del pool, sin tocarlo** — sin ningún modelo. Tres verbos, ids opacos re-sorteados por conversación, reglas de sitio aplicadas antes de mostrar la nota, una guarda que no consulta ninguna respuesta: $\text{requires}(n) \setminus \text{opened} \ne \emptyset$ | **72/72** recorridos del oráculo reproducidos como generaciones guionadas, 949 comandos, 0 rechazados, 0 mal formados, la guarda en silencio; tres recorridos que violan cortados en `strict`, continuados en `recover`; la compuerta falla con un enlace roto. La búsqueda es léxica y acá no se mide | M7-W2 |
| **Un buscador por palabras no encuentra una nota a partir de una pregunta que no usa sus palabras — el margen para el que existe el radar** — cero GPU, sin modelo. 94 paráfrasis de cabecera, una por nota, escritas después de congelar el diseño; solapamiento medio de palabras con el objetivo 0,039. $\text{recall@}3 = \lvert\{q : \operatorname{rank}_q \le 3\}\rvert / \lvert Q\rvert$ | recall@3 léxico **0,064** (6/94), 56 consultas no coinciden con ninguna nota; sobre los 72 enunciados 0,125. Un piso por construcción, así que la compuerta de R0 lleva además un 0,80 absoluto. El número propio de R0 es la fila siguiente | M7-W3 (margen) |
| **Un encoder estándar encuentra la nota que un buscador por palabras no — y no las veces suficientes.** Radar R0, `Qwen3-Embedding-0.6B`, $s(n\mid q)=\langle e(q),e_{\text{when}}\rangle+0{,}5\langle e(q),e_{\text{what}}\rangle$, las mismas 94 paráfrasis | recall@3 **0,638** contra 0,064 — **56 : 2** pareado — y por debajo del **0,80** fijado antes de correr: **W3 no pasa**. β = 0,5 le gana a sólo-`when:` 30 : 4. De 34 fallos, 13 en rango 4–6 (recall@10 0,830), 16 más allá del 10. El estándar queda; la brecha es de R1, sobre conjuntos nuevos | M7-W3 (R0) |
| **Se puede generar un corpus de recorridos manejando al árbitro, sin nada que memorizar y sin recorrer nunca al hermano** — cero GPU, sin modelo. $V(r)\cap N(r)=\varnothing$ para cada fila, $V$ los valores leídos de un hueco o de un `<calc>`, $N$ los números que dice el enunciado | 600 filas + las mismas 600 sin verbos; 80 recorridos del apartado y 60 de control. **0** valores en un enunciado, **0** casos evaluados en el corpus, **0** aperturas de las 17 notas propias de `discontinue-iv`, **0** filas no reproducidas byte por byte en `strict`; cada cláusula rota por un test. Un recorrido de 32 pasos no entra en 1 536 tokens: ventanas, con el estado llevado; la fila más larga, 1 298. **Si un modelo aprende el hábito es W5** | M7-W4 |
| **El brazo que mata de la memoria, corrido una vez: el brazo con biblioteca no le gana al base sin entrenar que lee.** Dos adaptadores sobre `Qwen3.5-4B`, con y sin la biblioteca, puntuados como recorridos sobre un procedimiento nunca recorrido en el entrenamiento; titular n = 56 | `withlib` **35**, `base-reads` **45**, `nolib` 2, `base-walks` 0. 35 : 2 sobre sin-biblioteca, 35 : 0 sobre el base navegando solo — **6 : 16 contra el base con las notas del oráculo, $p=0{,}052$: no pasa.** 12 de 21 fallas son el segundo de dos valores en una forma de nota que el corpus nunca mostró (0 de 108 filas de cantidad): 11/12 con el primer valor, 0/11 con el segundo. La navegación se transfirió — 0 fallos de recuperación, 3 verbos rechazados en 140 recorridos contra 368, control 58/60 contra 41/60 | M7-W5 |
| **…y la falla atribuida: el lector entrenado erra una forma que su corpus nunca mostró.** Los recorridos grabados de `withlib` reproducidos, el base pelado escribe la línea final (`composed`) | quedan **0 de las 12** fallas de cantidad con recorrido limpio; `composed` 41/54 = su techo sin GPU; 12 : 4 contra `withlib` ($p=0{,}077$, empate), 1 : 5 contra `base-reads`; `carry/middle-find` quedan 8 de 8. **No es un diseño de servicio:** en lo que al adaptador se le enseñó el base lee peor — control 39 contra 58 (0 : 19), línea compartida 8 contra 15 (0 : 7). Las dos notas de dos valores de la biblioteca están en el procedimiento retenido; 0 de 108 filas de cantidad leen dos valores. El veredicto de W5 queda | M7-W5b |
| **La fuente misma tiene la forma exactamente una vez fuera del procedimiento retenido — y se puede construir un corpus que le pone precio de un medio a "copiar el primer número".** Open RN cap. 23, leído línea por línea: ningún checklist entrenado afirma dos valores de una cantidad; la prosa del capítulo lo hace una vez (factores de goteo macro contra micro, verificado byte a byte contra la página) | corpus v2, cero GPU: 153/600 filas piden uno de los valores de una nota de dos valores — 30 sobre la nota real, 123 sobre oraciones agregadas por el sitio **declaradas inventadas** — condicional pedido 75/153, política del primer número 0,51; nota retenida abierta 0/600; 0/168 casos de evaluación nuevos es uno de los de W5; piso 6/66. Los adaptadores **todavía no están entrenados** | M7-W5c (compuerta) |
| **…y mostrar la forma no enseñó a leerla — el diagnóstico queda falsado.** Corpus v2, misma receta, sets retenido y de control nuevos, escritos después del congelamiento | valor condicional **4/15** (antes 0/11; Fisher a una cola $p=0{,}091$, banda fijada de antemano). El par de W5 sobre el titular nuevo: `withlib` 42 vs `base-reads` 46, **12 : 16, $p=0{,}57$ — un empate, no pasa**; 39 : 0 contra sin biblioteca, 42 : 0 contra el base caminando. 14 fallas de cantidad, todas con recorrido limpio, todas bien para el base sin entrenar: el adaptador escribe el primer número como un entero. En las ocho notas que entrenó lee la condición 17/18 — aprendió las notas, no la habilidad. La navegación mejoró: línea compartida 22/22 (base 8), control 78/80 (base 56) | M7-W5c |
| **Una política de respuesta — el adaptador camina, el base lee los valores — no le gana al adaptador solo en un set escrito después del congelamiento; y la razón es la consulta del adaptador.** Política y regla del tipo de tarea congeladas en su propio commit; sets apartado y de control nuevos | titular (67): política 42, withlib 37, base-reads 52 — **5 : 0, $p=0{,}0625$, un empate**; contra base-reads 6 : 16; el control aguanta 75 contra 73. Donde el recorrido abrió la nota que da el valor, el base lee **21/21**. Las otras 11 son fallas de búsqueda que el buscador no causó: ante una redacción nueva el adaptador escribe una **consulta de entrenamiento de otro tema**, textual en 9 de 11 (68 apariciones en su corpus), recorre el estante de goteos y calcula un goteo; el mismo buscador léxico, con el enunciado del pedido, lista la nota necesaria **16/16**. Sobre los sets de W5c, con 0 fallas: 56/66 (14 : 0; 12 : 2 contra base-reads), control 75/80 — una primera mirada fuera de muestra, no la afirmación | W5d |
| Los adaptadores de Qwen 3.5 son servibles: C18 era un desajuste de nombres | mismos pesos, 496 tensores renombrados: `not applied` → `applied`; activación real 0 → 152 de 178 módulos | D2 |

## 2. Lo que falló, y qué enseñó cada falla

| qué se intentó | qué pasó | corrida |
|---|---|---|
| **El experto que razona.** Un miembro de mecánica de fluidos, 600 cadenas supervisadas | sigue el protocolo a la perfección — 0 rechazos, 6–8 llamadas contra las 7 enseñadas — y se equivoca en la física en 78 de 90; pierde, pareado, contra una regla escrita a mano ($p = 0.022$). 600 ejemplos enseñaron el protocolo y no la física | P37–P40 |
| …leído donde ocurre, cuatro días después | de esas fallas, **74 de 79** tienen un cálculo con un número que no salió de ningún lado y dejan un resultado de herramienta sin usar — 75 (217 de 456 resultados ignorados): busca 882,3 y multiplica por 1359,7. **No es principalmente física equivocada — no usa lo que la herramienta devolvió.** Y esa corrida lo sirvió por `tool_calls`, no en la forma inline que su corpus enseñó; fluidos nunca se re-sirvió en modo corpus. *"El experto que razona falla" se sostiene como puntaje y queda abierto como explicación* | M7 brazo 0 |
| …**y después re-servido como enseñó su corpus — la línea de arriba queda retirada como explicación y como puntaje dentro de la región.** | 11/90 era el camino de `tool_calls`, no el experto: inline, **90/90** (§1). *"El experto que decide funciona, el experto que razona falla"* fue la línea más citada de este registro durante cuatro días, y describía un harness. Lo que sigue sin medir está fuera de la región: por debajo de su profundidad de entrenamiento (P45 pasó por el mismo camino y **vuelve a estar abierto**) y sobre familias hermanas (P14, un harness anterior) | M7 brazo 0b |
| …y por debajo de su profundidad de entrenamiento, se pasa de rosca | entrenado sólo en cadenas de 6 a 9 pasos: 18 de 18 se pasó de rosca por debajo de esa banda; en problemas de tres pasos, la base pelada le gana, 0,167 a 0,000. **Un corpus con una sola dificultad enseña un piso** | P45 |
| **Destilación desde un maestro de frontera** | transfiere el procedimiento, no la aritmética: π/4·0,22² da 0,037006 en vez de 0,038013. Adaptador + calculadora 40/40; base + calculadora 0/40 con 53 llamadas; adaptador solo 4/40 | P5–P7 |
| **La aceptación como un ranking de expertos** contra un único target más grande | la precondición falló dos veces: un target *sin entrenar* puntúa debajo del mejor experto, 0,746 < 0,989 y 0,967 < 1,000; donde el target es fuerte, los grados saturan (`g75` ≡ `g600` = 1,000). Cerrado sin veredicto | P55, P55b, P58 |
| **La aceptación contra una API de frontera** | imposible, no sólo cara: sin logprobs para una continuación forzada, un tokenizer distinto | P48 |
| **La aceptación a nivel de caracteres** | mide formato, no acuerdo: respuestas idénticas puntúan 0,00 entre formatos; la concordancia entre targets pasó de 1/5 → 4/5 sólo porque un target indenta | S0–S2 |
| **Una frontera adelante de un 12B local** en la primera suite | ninguna lo estuvo: `gemini-3.5-flash-lite` 13/20, `gemini-3.8-flash` 13/20, `gemini-3.1-pro-preview` 8/20, contra `gemma4:12b` 12/20 gratis. Pagar 100× más puntuó peor | S1 |
| **El ruteo por caso** con reglas de escalamiento escritas a mano | peor que por región: 0,378 y 0,689 contra 0,775. Las reglas buscan cadenas inconsistentes; este experto escribe cadenas que son **coherentes y equivocadas** | P41 |
| …el acuerdo con la frontera como señal por caso | funciona — de acuerdo: 8 de 8 correctos; en desacuerdo: 60 de 61 equivocados — pero llama a la frontera todas las veces, así que compra calidad y **ningún ahorro** | P41 |
| **La composición** de un adaptador de protocolo con un adaptador de dominio | nunca se midió limpiamente: los dos corpus enseñaron notaciones distintas y cada brazo se puntuó bajo un solo prompt. Apilado 0/30 y mezclado 0/30 son reales y no dicen nada causal. Estacionado | P8, P9, P13 |
| **El uso de herramientas como una sola capacidad** | es dos: la *disposición* a preguntar transfiere ampliamente (un kernel de física hace que la base estire la mano hacia herramientas de email en 123 de 150 casos — con los nombres equivocados, 127 rechazadas); el *vocabulario* transfiere angostamente (el kernel de email nunca es rechazado y pregunta en 22). Los tres brazos empatan en exactitud | P31, P34, P35 |
| **El router como un modelo de n-gramas del corpus de cada miembro** (hito 2, brazo 1) | seguro (fila en §1) e inusable: **120 de 120** pedidos legítimos de remitentes fuera de los pools del generador se mandan afuera, donde el diccionario no pierde ninguno — toda dirección generada termina en `.com`, así que `. com >` se volvió *frame*. **Aprendió la uniformidad del generador.** Y 0 de 240 paráfrasis de la pregunta de un miembro se recuperaron: para un modelo léxico una paráfrasis y una tarea distinta son la misma cosa. El brazo 2 es un modelo de embeddings | M2 |
| **El router como un modelo de embeddings de los corpus** (hito 2, brazo 2) — `Qwen3-Embedding-0.6B`, todo texto embebido como *la tarea que se pide* | más seguro que el diccionario sobre texto ajeno (15/338 servidos localmente contra 119), recupera 99/240 paráfrasis — y **pierde 120/120 pedidos de remitentes no vistos**, como el brazo 1. **No es calibración:** los remitentes no vistos (coseno mediano 0,89, máx 0,964) y *el listado propio de un miembro seguido de otra tarea* (0,87–0,91, máx 0,966) ocupan el mismo rango; un umbral que conserve el 95 % de los primeros sirve 219 de 338 textos ajenos. **Cambiar quién escribe mueve un pedido tanto como cambiar qué se pide.** Lo que queda: una proyección aprendida que factorice la tarea del contenido | M2 brazo 2 |
| **El router como embedding de `cactus-compute/needle`, pesos de fábrica** (hito 2, brazo 3, solo margen) — mismo evaluador que el brazo 2, solo cambia el codificador | **NO SEGURO**: 62 de 142 casos fuera de región servidos localmente (mismo contenido, tarea distinta, E/E2: 29/40, 33/40) — cerca de **10×** la tasa de fuga del brazo 2. Sí recupera tráfico real que el brazo 2 perdía por completo (remitentes no vistos, F: 19/40 contra 0/120) pero no de forma confiable (21/40 se siguen perdiendo). Sacrifica justo la propiedad que el router existe para garantizar; la cabeza calibrada y el ajuste fino local por los que se eligió este brazo son exactamente lo que los pesos de fábrica no ejercitan | M2 brazo 3 |
| **Una herramienta de búsqueda tasada sobre una tabla fija** | un control **sin ninguna capa de herramientas** puntuó 27/30: siete fluidos × dos propiedades son catorce números y 600 ejemplos los memorizan. *Una suite cuyas llamadas a herramientas se pueden recordar no puede tasar una capa de herramientas.* Corregido con un manual sorteado por caso — y es la regla que hereda cada base de conocimiento acá | P15, P21 |
| **Un especialista fuera de su región** | 30/30 en sus fórmulas adentro, **1/20** en dos familias hermanas que nunca vio, la prosa igual de fluida; los rechazos de la capa de herramientas no marcan el borde (0,18 afuera, 0,15 adentro; una guarda en 0,62 donde el azar era 0,60). Una relación inventada sí falla el análisis dimensional — una guarda que no consulta a ningún modelo | P14, P18, P22 |
| **Una máscara de gramática** sobre las llamadas a herramientas | compra prolijidad, no exactitud: rechazos 23 → 10, respuestas finales 5/30 → 4/30. De 30 fallas, 19 tenían cada llamada limpia | P8, P24 |
| **Una suite de código generada** | un adaptador memoriza formas: una cola por familia (180/180 completions held-out, textuales en el entrenamiento), después un esqueleto por (familia, corte), después ~60 formas cubiertas doce veces por 720 ejemplos. Se detuvo en tres | P52–P54 |
| **El experto de un tercero** de un hub de modelos | ninguno existe a este tamaño con herramientas y un verificador mecánico; en GSM8K la base ya está en 87,6 | P42 |
| **`Qwen3.5-4B` como base** bajo vLLM 0.29.0 | el adaptador se carga, se loguea, y se sirve la base. Leído durante cinco días como un límite del stack de serving; es nombres de tensores (§1, D2) | P33 |
| **Gemma 4 como base de PEFT** | `Gemma4ClippableLinear` no es `nn.Linear` | P29 |

## 3. Lo que las suites mismas resultaron ser

Las cuatro suites medidas alguna vez fallan al menos una de `training/suite_gates.py` — P50:
cada región de cada suite se sienta en exactamente una profundidad, así que región y dificultad son una
sola variable; la región se lee del prompt a 0,856 (fluidos) y 0,940 (triage);
la redacción de emails tiene una sola dificultad para los 180 casos. Y el hallazgo más profundo no tiene compuerta:
**una suite generada no puede contener una dificultad que nadie pensó.**

Un número de calibración tiene su propio techo, fijado por la información en la entrada: una brecha de AURC
de 0,400 leída como lugar para una cabeza tipada era 69–82 % la suite — P44, P46.

## 4. Instrumentos que mintieron

Cada uno produjo un número limpio y equivocado, y cada uno es ahora una regla en
[`../../CLAUDE.md`](../../CLAUDE.md) §3 o un test que falla.

| la mentira | cómo se descubrió |
|---|---|
| T4 reporta soporte bf16 que en realidad emula; la corrida puntuó 0/60, lo que se leyó como la falsificación del paso | capacidad de cómputo, no el flag — P2 |
| Gradient checkpointing dejado prendido durante la generación: 0/60 con él, 44/60 sin él | el brazo de control |
| Un corpus entrenado sobre el enunciado pelado, servido con el bloque de herramientas agregado: 71 rechazos de 606 que parecían física | el corpus tiene que enseñar el prompt que se sirve; los generadores *llaman* a `render_tools` — P38 |
| El mismo adaptador, los mismos casos, temperatura 0: 84, 81, 82 contra una compuerta de 83 | vLLM no es determinista de corrida a corrida; un veredicto se lee al lado del test pareado — P36/P38/P40 |
| "Le gana a la barra" dispara ~45 % de las veces por azar en un modelo que está parado en la barra | binomial exacto, y comparación pareada sobre fixtures compartidos (`bar.py`) |
| vLLM servido sin su parser de llamadas a herramientas: 240 de 240 pedidos HTTP 400, y la línea de progreso decía `correct 0` — lo que parece un modelo que no puede hacer la tarea | un brazo prueba que puede alcanzar sus herramientas antes de puntuar — P51 |
| 180/180 contra 56/180 de la base — y cada completion held-out estaba textual en el conjunto de entrenamiento | deduplicar sobre lo que se escribe, no sobre lo que se pregunta — P53 |
| Un `kb_pays` pre-registrado disparó 164 : 74 porque un default se había dado vuelta | la barra de mayoría lo guarda — P61 |
| Un miembro liberado sobre 60 casos con los pesos dejados en la tarjeta | el `eval_n` de la suite; el chain trae los adaptadores a casa — P64 intento 1 |
| `modules_with_weights: 531` para un adaptador que cayó en 0 de 178 módulos | vLLM activa tres LoRAs de relleno mientras perfila; 531 = 3 × 177. El chequeo de consistencia del brief lo atrapó — D2 |
| Tres miradas al mismo conjunto de prueba dijeron que el router era perfecto; un conjunto escrito **después** de que el diseño se congeló encontró que pierde todo pedido con pinta real | congelar, *después* escribir el conjunto que cuenta — M2 |
| Una regla de aceptación que preguntaba *quién está más cerca* antes de *quién reclama* perdió 60 de 240 pedidos ante un miembro que no reclamaba nada | el código hace lo que dice el brief, y un test lo sostiene — M2 |
| Una reproducción que se tragaba los errores de sus herramientas: cada `<lookup>` levantaba una excepción sobre un handbook pasado en su forma JSON, dentro de un `except` amplio, y dos de los tres conteos publicados estuvieron mal unas horas (75 y no 74 sin usar; 217 de 456 y no 132 de 371). La conclusión se sostuvo; los números no | los errores se cuentan y se reportan (`tool_errors`), nunca se saltean — M7 brazo 0 |
| **Una capacidad entera leída de un camino de servido.** Un experto entrenado sobre resultados escritos inline se puntuó por mensajes `tool_calls`; llamó a sus herramientas, nunca vio sus resultados donde había aprendido a leerlos, escribió números propios, y puntuó 11/90. Durante cuatro días eso fue *los modelos chicos no pueden razonar*. Mismo adaptador, inline: 90/90 | la falla se leyó donde ocurre (*¿se usó el resultado?*) y el único re-servido que separaba al modelo del camino se compró primero — M7 brazo 0, 0b |
| Una corrida que murió "porque una sonda de vida quedó sin respuesta" — mi diagnóstico, escrito en un brief — en realidad había sido terminada por Colab a los **sesenta minutos exactos**, dos veces (`10:45:28 → 11:45:43`, `11:59:34 → 12:59:34`). Y el arreglo mató la corrida siguiente: `$(grep … \| tail -1)` sale 1 bajo `pipefail` en el primer sondeo, y el trap de EXIT detuvo una A100 | leer `colab log -s <sesión>` antes de culpar al chain; la unidad de trabajo es una sesión; un test ahora corre esa línea de shell bajo las opciones del propio script — M1 |
| Guardas que leen el código fuente dispararon sobre prosa que describía la ausencia que verifican — cuatro veces | una guarda lee el código, no el archivo |
| `grep -c` imprime su cero y sale con 1; un rescate de pesos se saltó a sí mismo y costó un adaptador | `weights_in()` |
| **Un evaluador mal en los dos sentidos, atrapado antes de que nada se leyera a través de él.** El `verify` de W4 quería la línea renderizada de un paso como subcadena exacta, con el marcador `[site]` — una paráfrasis del paso correcto fallaba, en 57 de 80 filas apartadas — y tomaba cualquier número en cualquier parte: *"15 años … espera 8 segundos"* aprobaba un chequeo de 15 segundos. Lo encontró una revisión adversarial del PR, no una corrida | sólo la línea final; un número sólo pegado a su unidad; un paso por atribución entre las notas de paso; el recorrido en el registro del árbitro; `format` y `unread` como estados propios — `grade_walks.py`, M7-W4 rediseño 2 |
| **El vigía en vivo recortó una línea del veredicto.** La cadena muestra las últimas tres líneas que coinciden del log de la corrida; W5 imprimió cuatro de golpe y `withlib vs nolib` nunca llegó al log transmitido | no se perdió nada — los pares se leyeron del JSON, como todo veredicto — pero quien leyera sólo el log de la cadena habría visto dos pares de tres. El log es para la posición; el archivo es para el veredicto | M7-W5 |
| **Un puntaje de lectura leído antes de contar las fallas de búsqueda.** La política de W5d recuperó 5 de 16 fallas de valor y parecía un lector flojo; partida según si el recorrido había abierto la nota que da el valor, es 21/21 donde había nota y 0/11 donde no había — y "el buscador falló" también estaba mal: el buscador devolvió lo que pedía una consulta memorizada | contar las fallas de búsqueda, y leer la consulta que las causó, antes de leer un puntaje de lectura; una política puede estar bien y no poder medirse cuando el recorrido nunca encuentra la nota |

## 5. Trabajo relacionado, leído y no corrido **[read]**

- **Skill-to-LoRA** (arXiv 2606.16769): un `SKILL.md` convertido en un LoRA por skill sobre
  Qwen3.6-27B. 59 / 54 / 65 de 210 para sin skill / texto completo / adaptador — el signo de P61,
  con un efecto adentro del ruido ($z = 1.19$ y $0.65$, sin pareo, sin semillas). Un LoRA
  compartido entre skills perjudica. Su auto-destilación no transfiere a una base cuyo
  maestro base + documento hace cero llamadas a herramientas (P61).
- **Adaptive Minds** (arXiv 2510.15416): el modelo base lee metadata de adaptadores y nombra
  al miembro. El ruteo por palabra clave cae de 48,3 % a 5 adaptadores a 31,7 % a 30 — la
  falla predecible de un diccionario a medida que crece el pool, y la razón por la que existe el hito 2.
  Rutea por afinidad temática y por default cae a un adaptador general; acá una tabla medida decide
  qué se sirve localmente y el default es la frontera.
- **vLLM**: multi-LoRA sobre una base y atención en árbol ya se shippearon; un drafter adaptado con
  LoRA para decodificación especulativa es un RFC (#52038). Una cabeza de draft entrenada sobre los
  estados ocultos del target existe para el 32B y le gana a todo esto en latencia — por eso la
  aceptación acá nunca fue una afirmación de velocidad.
