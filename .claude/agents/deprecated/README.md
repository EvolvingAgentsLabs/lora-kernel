# Deprecated agents

An agent moves into this directory when its step is finished or its job turned out not to
exist. It keeps one line at the top saying what killed it, because the reason is the part
worth keeping.

| agent | retired | what killed it |
|---|---|---|
| [`alpha-runner`](alpha-runner.md) | 2026-09-19 | the character-level acceptance instrument it drove measured format, not agreement (S0), and left with the rewrite; acceptance is `accept_rank.py` through the chain |
